#include "print_alphabet_x10.h"
/**
*Prints 10 lines alphabets in lower case and adds a new line
*/
void print_alphabet_x10(void)
{
char a, b;
for (b=0; b<10; b++)
{
for (a=a; a<=z;a++)
{
_putchar(a);
}
_putchar(n);
}
}
